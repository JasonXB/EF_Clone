export interface EfLogoProps {
    className?: string;
    alt?: string;
    // added height as a prop so it can be changed depending on component its being used in.
    // can definitely change this again if it's not intuitive.
    height: number
  }
  